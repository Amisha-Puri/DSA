#include <iostream>
using namespace std;

int hashFunction(int key, int size) {
    return key % size;
}

int main() {

    int size = 10;
    int key = 25;

    int index = hashFunction(key, size);

    cout << "Hash Index: " << index;

    return 0;
}
